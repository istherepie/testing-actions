name: Create QA

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:

  create:
    runs-on: ubuntu-latest
    steps:
    - name: Create QA instance
      run: echo Setup QA for this particular feature
